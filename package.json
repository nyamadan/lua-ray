{
  "name": "lua-ray",
  "version": "1.0.0",
  "description": "lua ray",
  "keywords": [],
  "author": "Yoshiki YAMADA <nyamadandan@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "configure": "cmake --preset gcc-debug",
    "configure:win32": "cmake --preset win32-release",
    "configure:emscripten": "cmake --preset emscripten-release",
    "start": "python3 server-cross-origin.py",
    "build": "cmake --build --preset gcc-debug --target all",
    "build:win32": "cmake --build --preset win32-release --target all",
    "build:emscripten": "cmake --build --preset emscripten-release --target all",
    "test": "ctest --preset gcc-debug --output-on-failure"
  },
  "devDependencies": {
    "prettier": "3.8.1"
  }
}
