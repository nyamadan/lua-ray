name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive

      - name: Restore ccache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ github.workspace }}/.ccache
          key: cache-ccache-${{ runner.os }}-${{ github.ref_name }}

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "lts/*"
          cache: "pnpm"

      - name: Install Node dependencies
        run: pnpm install

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            gnome-desktop-testing \
            libasound2-dev \
            libpulse-dev \
            libaudio-dev \
            libfribidi-dev \
            libjack-dev \
            libsndio-dev \
            libx11-dev \
            libxext-dev \
            libxrandr-dev \
            libxcursor-dev \
            libxfixes-dev \
            libxi-dev \
            libxss-dev \
            libxtst-dev \
            libxkbcommon-dev \
            libdrm-dev \
            libgbm-dev \
            libgl1-mesa-dev \
            libgles2-mesa-dev \
            libegl1-mesa-dev \
            libdbus-1-dev \
            libibus-1.0-dev \
            libudev-dev \
            libthai-dev \
            libpipewire-0.3-dev \
            libwayland-dev \
            libdecor-0-dev \
            liburing-dev \
            mingw-w64-x86-64-dev \
            gcc-mingw-w64-x86-64 \
            g++-mingw-w64-x86-64 \
            ccache

      - name: Configure (GCC)
        run: pnpm run configure

      - name: Build (GCC)
        run: pnpm run build -j2

      - name: Test
        run: pnpm run test

      - name: Setup Emscripten
        run: |
          if [ ! -d "${{ github.workspace }}/.emsdk" ]; then
            git clone https://github.com/emscripten-core/emsdk.git ./.emsdk
          fi
          cd ./.emsdk
          ./emsdk install 6.0.5
          ./emsdk activate 6.0.5
          source ./emsdk_env.sh

      - name: Configure (Emscripten)
        run: pnpm run configure:emscripten

      - name: Build (Emscripten)
        run: pnpm run build:emscripten -j2

      - name: Prepare Emscripten artifacts
        run: |
          mkdir -p artifacts/emscripten
          cp server-cross-origin.py artifacts/emscripten/
          cp build/emscripten-release/lua-ray.html artifacts/emscripten/
          cp build/emscripten-release/lua-ray.js artifacts/emscripten/
          cp build/emscripten-release/lua-ray.wasm artifacts/emscripten/
          cp build/emscripten-release/lua-ray.data artifacts/emscripten/

      - name: Upload Emscripten artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: emscripten-build
          path: artifacts/emscripten
          if-no-files-found: error

      - name: Configure (Win32)
        run: pnpm run configure:win32

      - name: Build (Win32)
        run: pnpm run build:win32 -j2

      - name: Prepare Win32 artifacts
        run: |
          mkdir -p artifacts/win32
          cp build/win32-release/lua-ray.exe artifacts/win32/
          cp main.lua artifacts/win32/
          cp -r workers artifacts/win32/
          cp -r assets artifacts/win32/
          cp -r lib artifacts/win32/
          cp -r scenes artifacts/win32/

      - name: Upload Win32 artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: win32-build
          path: artifacts/win32
          if-no-files-found: error
